import React from 'react';
import { SafeAreaView } from 'react-native';
import TMenu from '../templates/menu/TMenu';

const Menu = () => {
  return (
    <SafeAreaView>
      <TMenu />
    </SafeAreaView>
  );
};

export default Menu;
